{
  "name": "checkin-backend",
  "version": "1.0.0",
  "description": "Backend for the Checkin application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cp server.js index.js && cp package.json package.json.bak && echo '{\"main\":\"index.js\"}' > package.json"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  }
}
